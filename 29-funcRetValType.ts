// let userRole = 'admin' | 'editor' | 'reader';

// function access(userRole) {

// }

function add(a: number, b: number): number {
  // return "hello"; // error

  return 1;
}

// add("string",1);  //error
